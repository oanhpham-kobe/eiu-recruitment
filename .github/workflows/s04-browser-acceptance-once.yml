name: S04 Interview Browser Acceptance Once

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily paused while the shared StatusMenu repair is staged."
