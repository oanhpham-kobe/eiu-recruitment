name: S06-002 S06-001 Fixture Repair

on:
  push:
    branches:
      - control/S06-002-s06-001-fixture-repair-6dd0b03

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout guarded task branch
        uses: actions/checkout@v4.2.2
        with:
          ref: oanhpham-kobe/TASK-S06-002-user-rbac-identity
          fetch-depth: 0

      - name: Verify parent and align S06-001 owner fixture
        shell: bash
        run: |
          set -euo pipefail
          EXPECTED_PARENT='6dd0b037fb1e45276e3d5a2b4ecf54f8cc16694a'
          test "$(git rev-parse HEAD)" = "$EXPECTED_PARENT"
          python - <<'PY'
          from pathlib import Path

          path = Path('supabase/tests/master_data_lifecycle_history_test.sql')
          text = path.read_text()
          old = """  insert into public.app_user_permissions(app_user_id, permission_code)\n  values\n    (v_hr, 'master_data.manage'),\n    (v_hr2, 'master_data.manage'),\n    (v_hr, 'interviews.view'),\n    (v_hr, 'interviews.status')\n  on conflict do nothing;\n"""
          new = """  insert into public.app_user_roles(app_user_id, role_code)\n  values (v_hr, 'HR')\n  on conflict do nothing;\n\n  insert into public.app_user_permissions(app_user_id, permission_code)\n  values\n    (v_hr, 'master_data.manage'),\n    (v_hr2, 'master_data.manage'),\n    (v_hr, 'interviews.view'),\n    (v_hr, 'interviews.status')\n  on conflict do nothing;\n"""
          if text.count(old) != 1:
              raise SystemExit(f'expected exactly one fixture block, found {text.count(old)}')
          path.write_text(text.replace(old, new, 1))
          PY
          git diff --check
          test "$(git diff --name-only | wc -l)" -eq 1
          test "$(git diff --name-only)" = 'supabase/tests/master_data_lifecycle_history_test.sql'

      - name: Commit and push append-only repair
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'oanhpham-kobe'
          git config user.email 'oanh.pham@eiu.edu.vn'
          git add supabase/tests/master_data_lifecycle_history_test.sql
          git commit -m 'fix(test): align S06-001 owner fixture with HR eligibility'
          git push origin HEAD:oanhpham-kobe/TASK-S06-002-user-rbac-identity
