name: DS-006 Acceptance Once

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  acceptance:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DS-006 push gate temporarily paused for focused test repair"
