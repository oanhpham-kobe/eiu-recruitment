# PROJECT_CONTROL_BOOTSTRAP

Implementation-governance bootstrap and reference template for **EIU Recruitment**.

- **Authority:** EIU Full Handover v1.17 remains the authority for WHAT the system must do.
- **Role:** These bootstrap files represent the reference template reflecting the current pre-commit baseline. Live implementation state is maintained under `D:/orca/recruitment/project_control/`.
- **Pre-Commit State:** Git is initialized on branch `main` at `D:/orca/recruitment`. The pre-commit baseline is staged in Git. The authoritative current staged count is recorded in CURRENT_STATE and must be re-observed from Git before execution. The first baseline commit is pending explicit authorization.
- **Portability:** Full Handover source files resolve relative to `SOURCE_ROOT = recruitment_webapp`.
- **Session Protocol:** Future sessions must run Resume Project Protocol and inspect live `project_control/` before modifying code.
